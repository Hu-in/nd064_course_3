namespace:
  name: prod

service:
  port: 4111

image:
  pullPolicy: Always

replicaCount: 3

resources:
  requests:
    cpu: "500m"
    memory: "128Mi"
  limits:
    cpu: "1000m"
    memory: "256Mi"
